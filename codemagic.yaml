workflows:
  ios-preview:
    name: ESIM Campus iOS Force Build
    max_build_duration: 60
    instance_type: mac_mini_m2
    environment:
      node: 18
      xcode: latest
      cocoapods: default
      vars:
        XCODE_WORKSPACE: "ios/Esimcampus.xcworkspace"
        XCODE_SCHEME: "Esimcampus"
    scripts:
      - name: Install dependencies
        script: |
          npm install --legacy-peer-deps
      - name: Surgical Dependency Repair
        script: |
          echo "Executing Force Repair on Objective-C dependencies..."
          
          # 1. Broad Fix for Date Picker (Casts all occurrences of ValueChanged events)
          find node_modules/react-native-date-picker -name "*.mm" -o -name "*.m" | xargs sed -i '' 's/UIControlEventValueChanged/(UIControlEvents)UIControlEventValueChanged/g' || true
          
          # 2. Header Injection for Date Picker
          DP_FILE="node_modules/react-native-date-picker/ios/RNDatePickerManager.mm"
          if [ -f "$DP_FILE" ]; then
             if ! grep -q "UIKit/UIKit.h" "$DP_FILE"; then
                echo '#import <UIKit/UIKit.h>' > "$DP_FILE.tmp"
                cat "$DP_FILE" >> "$DP_FILE.tmp"
                mv "$DP_FILE.tmp" "$DP_FILE"
             fi
          fi

          # 3. Broad Fix for RNScreens Orientation Traits
          find node_modules/react-native-screens -name "*.mm" -o -name "*.m" | xargs sed -i '' 's/@implementation RNSScreenWindowTraits/#import <UIKit\/UIKit.h>\n@implementation RNSScreenWindowTraits/g' || true

          # 4. Clean stray XML references
          find . -name "*.storyboard.xml" -delete || true
          find ios/Esimcampus -name "*.xml" -delete || true

      - name: Install CocoaPods
        script: |
          cd ios && pod install --repo-update
      - name: Xcode Project Hard-Reset
        script: |
          echo "Injecting mandatory stability flags..."
          ruby -e "
          require 'xcodeproj'
          project = Xcodeproj::Project.open('ios/Esimcampus.xcodeproj')
          
          project.targets.each do |target|
            target.build_phases.each do |phase|
              phase.files.each do |bf|
                if bf.file_ref && bf.file_ref.respond_to?(:path) && bf.file_ref.path && bf.file_ref.path.end_with?('.xml')
                   phase.remove_build_file(bf)
                end
              end
            end
            
            target.build_configurations.each do |config|
              config.build_settings['CODE_SIGNING_ALLOWED'] = 'NO'
              config.build_settings['CODE_SIGNING_REQUIRED'] = 'NO'
              config.build_settings['CLANG_CXX_LANGUAGE_STANDARD'] = 'c++17'
              config.build_settings['GCC_WARN_INHIBIT_ALL_WARNINGS'] = 'YES'
              config.build_settings['OTHER_LDFLAGS'] = '$(inherited) -Wl,-ld_classic'
              config.build_settings['OTHER_CPLUSPLUSFLAGS'] = '$(inherited) -w -D_LIBCPP_ENABLE_CXX20_REMOVED_TYPE_TRAITS=1'
              config.build_settings['OTHER_CFLAGS'] = '$(inherited) -w -Wno-error=conversion'
            end
          end
          project.save
          "
      - name: Build iOS Application (Maximum Force Mode)
        script: |
          # Use xcodebuild with maximal error suppression
          xcodebuild \
            -workspace "$XCODE_WORKSPACE" \
            -scheme "$XCODE_SCHEME" \
            -configuration Release \
            -sdk iphoneos \
            -archivePath build/Esimcampus.xcarchive \
            archive \
            CODE_SIGNING_ALLOWED=NO \
            CODE_SIGNING_REQUIRED=NO \
            CLANG_CXX_LANGUAGE_STANDARD="c++17" \
            GCC_WARN_INHIBIT_ALL_WARNINGS=YES \
            OTHER_CPLUSPLUSFLAGS='$(inherited) -w -D_LIBCPP_ENABLE_CXX20_REMOVED_TYPE_TRAITS=1' \
            OTHER_LDFLAGS='$(inherited) -Wl,-ld_classic' \
            COMPILER_INDEX_STORE_ENABLE=NO
          
          # Packaging
          mkdir -p build/ios/ipa
          if [ -d "build/Esimcampus.xcarchive/Products/Applications/Esimcampus.app" ]; then
            cd build/Esimcampus.xcarchive/Products/Applications
            mkdir Payload
            cp -r Esimcampus.app Payload/
            zip -r ../../../ios/ipa/Esimcampus_Preview.ipa Payload
          else
            echo "BUILD FAILED: Binary not found."
            exit 1
          fi
    artifacts:
      - build/ios/ipa/*.ipa
